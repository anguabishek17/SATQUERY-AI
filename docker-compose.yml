services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data
    environment:
      - LOW_CONFIDENCE_THRESHOLD=0.55

  frontend:
    build: ./frontend
    ports:
      - "4173:4173"
    depends_on:
      - backend
